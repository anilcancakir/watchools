00:00 +0: loading /Users/anilcan/Code/watchools/.claude/worktrees/playback-lifecycle/test/app/protocol/xtream/xtream_credentials_test.dart
00:00 +0: base URL normalisation strips a trailing slash, because every later call concatenates onto it
00:00 +1: base URL normalisation keeps a panel path prefix, minus its trailing slash
00:00 +2: base URL normalisation rejects a base URL Http would concatenate onto the driver base_url
00:00 +3: base URL normalisation accepts https as readily as http
00:00 +4: base URL normalisation rejects credentials in the authority, which toString would print in full
00:00 +5: base URL normalisation rejects without naming the value, so a pasted password never reaches a log
00:00 +6: Vault round trip save() writes exactly one key and load() returns an equal record
00:00 +7: Vault round trip clear() removes the one key it wrote
00:00 +8: Vault round trip load() is null on a vault that never held a credential
00:00 +9: Vault round trip load() normalises the stored base URL the way the constructor does
00:00 +10: Vault round trip load() throws on a payload that is not a credential object
00:00 +11: Vault round trip load() reads the exact four-key blob the previous version wrote, resolver null
00:00 +12: Vault round trip load() reads resolver null, throwing neither FormatException nor TypeError, when it is a number
00:00 +13: Vault round trip save() then load() round-trips a resolver value
00:00 +14: Vault round trip save() omits the resolver key entirely when the setting is the system one
00:00 +15: Vault round trip save() then load() round-trips a backgroundPlayback value
00:00 +16: Vault round trip load() reads backgroundPlayback null, throwing neither FormatException nor TypeError, when it is a number
00:00 +17: Vault round trip save() omits the background_playback key entirely when the setting is the system default
00:00 +18: Vault round trip load() throws when a field is missing or not a string
00:00 +19: Vault round trip load() throws without naming the field values, so nothing leaks
00:00 +20: fromEnvironment a build given no define carries no credential
00:00 +21: fromEnvironment builds the record when all three are given
00:00 +22: fromEnvironment defaults the user agent, which is not a secret and is always needed
00:00 +23: fromEnvironment refuses a partial set rather than half-honouring it
00:00 +24: fromEnvironment a bad base URL throws rather than addressing the wrong server
00:00 +25: equality two records differing only in backgroundPlayback are not equal
00:00 +26: toString names the provider and the user, and redacts the password
00:00 +27: toString prints backgroundPlayback literally, since it names no secret
00:00 +28: toString redacts a custom resolver, whose secret can sit in the path
00:00 +29: toString says the resolver is null when it is, rather than redacting nothing
00:00 +30: redact covers a whole stream URL, its query and its userInfo
00:00 +31: redact strips both secrets out of the FFmpeg reconnect warning that is the only lapse signal
00:00 +32: redact replaces every occurrence, since a retry line names the same URL twice
00:00 +33: redact leaves a line naming no secret exactly as it arrived
00:00 +34: redact redacts the password before the username, so a password containing the username still goes
00:00 +35: redact a provider that issues the password as the username redacts once, not twice over
00:00 +36: redact redacts the percent-encoded spelling a URL path carries
00:00 +37: redact redacts the query spelling too, which writes a space as + rather than %20
00:00 +38: redact cleans a URL this app actually built, not only one written by hand
00:00 +39: redact strips the base64 token a 302 puts in the path, which no literal spelling reaches
00:00 +40: redact strips a token whose signature is binary and whose base64 is percent-escaped
00:00 +41: redact leaves an encoded run that names no secret exactly as it arrived
00:00 +42: redact an empty secret is skipped, because replaceAll of it matches between every character
00:00 +43: All tests passed!
