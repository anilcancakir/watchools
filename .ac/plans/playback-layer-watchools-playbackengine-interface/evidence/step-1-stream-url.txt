00:00 +0: loading /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_stream_url_test.dart
00:00 +0: live builds /live/<user>/<pass>/<id>.ts against the account and the channel
00:00 +1: live keeps a panel path prefix, since the credential rides after it
00:00 +2: live a trailing slash on the base URL cannot produce a double slash
00:00 +3: live encodes a credential segment rather than letting it restructure the path
00:00 +4: extension prefers ts when the account and the channel both serve it
00:00 +5: extension yields m3u8 when the channel excludes ts, as channel 07 does
00:00 +6: extension falls back to the account list alone on an empty channel list
00:00 +7: extension is null when the channel and the account share no format
00:00 +8: extension is null when the account permits nothing this app can address
00:00 +9: extension follows the caller preference order rather than a hardcoded one
00:00 +10: extension honours the preference order even when it names one format only
00:00 +11: stream id is null for a channel with no provider identity
00:00 +12: All tests passed!
Resolving dependencies...
Downloading packages...
  code_assets 1.2.1 (2.0.0 available)
< file_picker 11.0.3 (was 12.2.0) (12.2.0 available)
  flutter_secure_storage 10.3.1 (11.0.0 available)
  flutter_secure_storage_darwin 0.3.2 (0.4.1 available)
< flutter_secure_storage_windows 4.1.0 (was 4.2.2) (4.2.2 available)
* fluttersdk_artisan 0.0.14 (was 0.0.14 from path /Users/anilcan/Code/fluttersdk/artisan)
* fluttersdk_dusk 0.0.13 (was 0.0.13 from path /Users/anilcan/Code/fluttersdk/dusk)
* fluttersdk_telescope 0.0.5 (was 0.0.5 from path /Users/anilcan/Code/fluttersdk/telescope)
* fluttersdk_wind 1.5.2 (was 1.5.3 from path /Users/anilcan/Code/fluttersdk/wind)
* fluttersdk_wind_diagnostics_contracts 1.1.0 (was 1.1.0 from path /Users/anilcan/Code/fluttersdk/wind_diagnostics_contracts)
  go_router 17.5.0 (18.0.1 available)
  hooks 2.0.2 (2.2.0 available)
* magic 0.0.9 (was 0.0.9 from path /Users/anilcan/Code/fluttersdk/magic)
* magic_devtools 0.0.4 (was 0.0.4 from path /Users/anilcan/Code/fluttersdk/magic_devtools)
  material_color_utilities 0.13.0 (0.13.1 available)
  meta 1.18.3 (1.19.0 available)
  native_toolchain_c 0.19.2 (0.19.4 available)
  objective_c 9.5.0 (9.6.0 available)
< package_info_plus 9.0.1 (was 10.2.1) (10.2.1 available)
< package_info_plus_platform_interface 3.2.1 (was 4.1.0) (4.1.0 available)
  platform 3.1.6 (3.2.0 available)
  pointycastle 3.9.1 (4.0.0 available)
  record_use 0.6.0 (1.1.1 available)
< share_plus 12.0.2 (was 13.3.0) (13.3.0 available)
< share_plus_platform_interface 6.1.0 (was 7.2.0) (7.2.0 available)
  test_api 0.7.12 (0.7.14 available)
  vector_math 2.4.0 (2.4.2 available)
< wakelock_plus 1.5.2 (was 1.8.0) (1.8.0 available)
< win32 5.15.0 (was 6.4.0) (6.4.0 available)
  xml 6.6.1 (7.0.1 available)
These packages are no longer being depended on:
- android_file_picker 1.1.0
- ffi_leak_tracker 0.1.2
- file_picker_darwin 1.1.0
- file_picker_linux 1.1.0
- file_picker_platform_interface 3.3.0
- file_picker_web 3.1.0
- windows_file_picker 1.2.0
Changed 22 dependencies!
23 packages have newer versions incompatible with dependency constraints.
Try `flutter pub outdated` for more information.
Analyzing 2 items...                                            
No issues found! (ran in 4.4s)
0
29:abstract final class XtreamStreamUrl {
64:  static Uri? live({
00:00 +0: loading /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart
00:00 +0: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: the generated catalogue is not empty
00:00 +1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: Artwork renders at desktop
00:00 +2: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: Artwork renders at desktop
00:00 +3: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: Artwork renders at desktop
00:00 +4: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: Artwork renders at desktop
00:00 +5: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: Artwork renders at desktop
00:00 +6: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: Artwork renders at desktop
00:00 +7: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: Artwork renders at desktop
00:00 +8: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: Artwork renders at desktop
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:00 +9: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/components/provider_notice_test.dart: every fault says something different, and says it in three parts
00:00 +9: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: Artwork renders on a phone
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:00 +10: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/components/provider_notice_test.dart: every fault says something different, and says it in three parts
00:00 +10: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: ChannelMark renders at desktop
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:00 +11: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/components/provider_notice_test.dart: every fault says something different, and says it in three parts
00:00 +11: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: ChannelMark renders on a phone
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:00 +12: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/components/provider_notice_test.dart: every fault says something different, and says it in three parts
00:00 +12: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: EpisodeRow renders at desktop
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:00 +13: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/components/provider_notice_test.dart: every fault says something different, and says it in three parts
00:00 +13: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: EpisodeRow renders on a phone
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:00 +14: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/components/provider_notice_test.dart: every fault says something different, and says it in three parts
00:00 +14: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: FactChip renders at desktop
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:00 +15: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/components/provider_notice_test.dart: every fault says something different, and says it in three parts
00:00 +15: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: FactChip renders on a phone
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:00 +16: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/components/provider_notice_test.dart: every fault says something different, and says it in three parts
00:00 +16: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: FavouriteButton renders at desktop
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:00 +17: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/components/provider_notice_test.dart: every fault says something different, and says it in three parts
00:00 +17: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: FavouriteButton renders on a phone
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:00 +18: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/components/provider_notice_test.dart: every fault says something different, and says it in three parts
00:00 +18: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:00 +18: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: PlayProgress renders at desktop
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:00 +19: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:00 +20: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:00 +20: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: PlayProgress renders on a phone
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:00 +21: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:01 +21: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: ProviderNotice renders at desktop
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +22: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:01 +23: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:01 +24: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:01 +24: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: ProviderNotice renders on a phone
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +25: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:01 +26: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:01 +26: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: StatusBadge renders at desktop
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +27: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:01 +28: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:01 +28: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: StatusBadge renders on a phone
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +29: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:01 +29: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: TitlePoster renders at desktop
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +30: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:01 +31: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:01 +32: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:01 +32: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: TitlePoster renders on a phone
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +33: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:01 +33: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice alone when the fault AND the result are both empty
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +34: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:01 +35: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:01 +35: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi still renders the empty state when there is no fault
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +36: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:01 +36: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi routes the notice action through GuideController.reload
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +36: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +37: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:01 +37: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Zaman renders the notice and keeps the toolbar when the session carries a fault
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +38: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:01 +38: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Zaman renders the notice alone when the fault AND the result are both empty
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +39: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:01 +39: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Zaman still renders the empty state when there is no fault
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +40: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:01 +40: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Zaman routes the notice action through GuideController.reload
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +41: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:01 +41: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: Vitrin renders the notice and keeps the toolbar when the session carries a fault
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +42: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:01 +42: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: Vitrin renders the notice alone when the fault AND the result are both empty
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +43: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:01 +43: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: Vitrin still renders the empty state when there is no fault
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +44: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:01 +44: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: Vitrin routes the notice action through LibraryController.reload
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +45: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:01 +45: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: Perde renders the notice when the session carries a fault, even with a title selected
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +46: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:01 +46: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: Perde renders the notice alone when the fault AND the catalogue are both empty
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +47: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: Perde renders the notice alone when the fault AND the catalogue are both empty
00:01 +48: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders an empty result set at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +48: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: Perde still renders the no-selection arm when there is no fault
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +49: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders an empty result set at 1440
00:01 +49: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: Perde routes the notice action through LibraryController.reload
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:02 +50: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders an empty result set at 1440
00:02 +51: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders an empty favourites filter at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:02 +52: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:02 +53: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +53: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a channel with no guide selected at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:02 +53: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +54: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +55: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +56: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +57: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +58: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +59: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +60: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +61: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +62: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +63: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +64: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +65: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +66: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +67: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +68: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +69: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +70: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +71: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +72: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +73: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +73: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:02 +73: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:02 +74: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +74: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:02 +75: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +75: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty result set at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:02 +76: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +76: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty favourites filter at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:02 +77: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:02 +77: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:03 +78: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:03 +78: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:03 +79: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_test.dart: CatalogueStore, the schema and the round trip round-trips 1000 channels as equal value objects
00:03 +79: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: and survives the way back to results
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
00:03 +80: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 1440
00:03 +80: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: and survives the way back to results
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:03 +81: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 1440
00:03 +82: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 1440
00:03 +83: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 1440
00:03 +84: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 1440
00:03 +85: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 1440
00:03 +86: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:03 +87: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:03 +88: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:03 +88: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders with the other view selected at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:03 +89: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:03 +90: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:03 +91: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:03 +92: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:03 +93: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:03 +94: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:03 +95: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:03 +96: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:03 +97: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:03 +98: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:03 +99: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:03 +99: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:03 +100: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
Shell: SCALE channels: 2976 rows in 48 ms
00:03 +101: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
Shell: SCALE titles:   38247 rows in 175 ms
Shell: SCALE combined: 223 ms (redesign threshold, not asserted: ~2000 ms)
00:03 +101: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders an empty result set at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:03 +101: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Vitrin survives it too
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:03 +102: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:03 +102: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders an empty favourites filter at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:03 +103: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:03 +103: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:03 +104: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:03 +104: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a channel with no guide selected at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
00:03 +105: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:03 +105: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a channel with no guide selected at 414
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:03 +105: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Zaman, the control, was never affected
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:03 +106: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a channel with no guide selected at 414
00:03 +107: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Zaman, the control, was never affected
00:03 +107: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:04 +108: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Zaman, the control, was never affected
00:04 +108: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:04 +109: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:04 +110: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty result set at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:04 +111: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty result set at 414
00:04 +112: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty result set at 414
00:04 +113: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty result set at 414
00:04 +114: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty result set at 414
00:04 +115: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty result set at 414
00:04 +116: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty result set at 414
00:04 +117: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty result set at 414
00:04 +118: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty result set at 414
00:04 +119: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty result set at 414
00:04 +120: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty result set at 414
00:04 +121: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/models/title_item_test.dart: TitleItem toggling a favourite keeps every other field
00:04 +122: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty favourites filter at 414
00:04 +123: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty favourites filter at 414
00:04 +124: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty favourites filter at 414
00:04 +125: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty favourites filter at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
00:04 +126: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty favourites filter at 414
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:04 +127: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty favourites filter at 414
00:04 +128: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty favourites filter at 414
00:04 +129: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty favourites filter at 414
00:04 +130: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty favourites filter at 414
00:04 +131: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty favourites filter at 414
00:04 +132: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:04 +133: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:04 +134: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders with the other view selected at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:04 +135: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders the whole catalogue at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:04 +136: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:04 +137: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:04 +138: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:04 +139: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders the whole catalogue at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:05 +140: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_account_test.dart: XtreamAccount.fromHandshake / active demo: auth 1, status Active, future exp_date is active
00:05 +141: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 414
00:05 +142: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:05 +143: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 414
00:05 +144: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 414
00:05 +145: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 414
00:05 +146: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 414
00:05 +147: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 414
00:05 +148: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 414
00:05 +149: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 414
00:05 +150: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 414
00:05 +151: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 414
00:05 +152: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 414
00:05 +153: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 414
00:05 +154: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 414
00:05 +155: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 414
00:05 +156: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 414
00:05 +157: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 414
00:05 +158: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 414
00:05 +159: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_account_test.dart: classifyProviderFault an account that failed the disjunction is expired
00:05 +160: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 414
00:05 +161: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 414
00:05 +162: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 414
00:05 +163: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:05 +164: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 414
00:05 +165: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 414
00:05 +166: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 414
00:05 +167: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 414
00:05 +168: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 414
00:05 +169: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 414
00:05 +170: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 414
00:05 +171: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 414
00:05 +172: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 414
00:05 +173: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 414
00:05 +174: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 414
00:05 +175: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 414
00:05 +176: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 414
00:05 +177: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:05 +178: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:05 +179: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +180: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +181: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +182: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +183: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +184: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +185: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +186: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +187: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +188: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +189: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +190: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +191: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +192: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +193: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +194: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +195: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +196: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +197: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +198: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +199: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +200: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +201: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +202: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +203: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +204: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +205: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +206: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +207: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +208: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +209: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +210: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +211: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
00:05 +212: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a film at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:05 +213: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a title with nothing filled in at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:05 +214: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:05 +215: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a film at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:05 +216: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a title with nothing filled in at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:05 +217: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen every season of a series renders
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:06 +218: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_credentials_test.dart: base URL normalisation strips a trailing slash, because every later call concatenates onto it
00:06 +219: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_stream_url_test.dart: live builds /live/<user>/<pass>/<id>.ts against the account and the channel
00:06 +220: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_stream_url_test.dart: live builds /live/<user>/<pass>/<id>.ts against the account and the channel
00:06 +221: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_stream_url_test.dart: live builds /live/<user>/<pass>/<id>.ts against the account and the channel
00:06 +222: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_stream_url_test.dart: live builds /live/<user>/<pass>/<id>.ts against the account and the channel
00:06 +223: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_stream_url_test.dart: live builds /live/<user>/<pass>/<id>.ts against the account and the channel
00:06 +224: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_stream_url_test.dart: live builds /live/<user>/<pass>/<id>.ts against the account and the channel
00:06 +225: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_stream_url_test.dart: live builds /live/<user>/<pass>/<id>.ts against the account and the channel
00:06 +226: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_credentials_test.dart: Vault round trip clear() removes the one key it wrote
00:06 +227: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_credentials_test.dart: Vault round trip clear() removes the one key it wrote
00:06 +228: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_stream_url_test.dart: live a trailing slash on the base URL cannot produce a double slash
00:06 +229: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_stream_url_test.dart: live a trailing slash on the base URL cannot produce a double slash
00:06 +230: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_credentials_test.dart: Vault round trip load() normalises the stored base URL the way the constructor does
00:06 +231: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_stream_url_test.dart: live encodes a credential segment rather than letting it restructure the path
00:06 +232: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_credentials_test.dart: Vault round trip load() throws on a payload that is not a credential object
00:06 +233: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_credentials_test.dart: Vault round trip load() throws on a payload that is not a credential object
00:06 +234: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_stream_url_test.dart: extension yields m3u8 when the channel excludes ts, as channel 07 does
00:06 +235: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_credentials_test.dart: Vault round trip load() throws when a field is missing or not a string
00:06 +236: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_stream_url_test.dart: extension falls back to the account list alone on an empty channel list
00:06 +237: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_stream_url_test.dart: extension is null when the channel and the account share no format
00:06 +238: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_credentials_test.dart: Vault round trip load() throws without naming the field values, so nothing leaks
00:06 +239: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_stream_url_test.dart: extension is null when the account permits nothing this app can address
00:06 +240: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_credentials_test.dart: toString names the provider and the user, and redacts the password
00:06 +241: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_stream_url_test.dart: extension follows the caller preference order rather than a hardcoded one
00:06 +242: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_credentials_test.dart: describe drops the query and the credential path segments of a stream URL
00:06 +243: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_credentials_test.dart: describe drops the query and the credential path segments of a stream URL
00:06 +244: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_stream_url_test.dart: stream id is null for a channel with no provider identity
00:06 +245: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_credentials_test.dart: describe keeps scheme, host, port and the segments that are not a credential
00:06 +246: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_credentials_test.dart: describe drops userInfo, the other place a URL carries a credential
00:06 +247: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_credentials_test.dart: redact strips both secrets out of the FFmpeg reconnect warning that is the only lapse signal
00:06 +248: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_credentials_test.dart: redact replaces every occurrence, since a retry line names the same URL twice
00:06 +249: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: no watchools credential reaches the panel the registered provider driver ships no Authorization header with a token cached
00:06 +250: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: no watchools credential reaches the panel the registered provider driver ships no Authorization header with a token cached
00:06 +251: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: no watchools credential reaches the panel the registered provider driver ships no Authorization header with a token cached
00:06 +252: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: no watchools credential reaches the panel the registered provider driver ships no Authorization header with a token cached
00:06 +253: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: no watchools credential reaches the panel the registered provider driver ships no Authorization header with a token cached
00:06 +254: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: no watchools credential reaches the panel the registered provider driver ships no Authorization header with a token cached
00:06 +255: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: no watchools credential reaches the panel the registered provider driver ships no Authorization header with a token cached
00:06 +256: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: no watchools credential reaches the panel the same request through a driver carrying AuthInterceptor does ship it
00:06 +257: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: no watchools credential reaches the panel the registered provider driver carries no interceptor and follows no redirect
00:06 +258: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: no watchools credential reaches the panel a 302 is surfaced rather than followed
00:06 +259: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: no watchools credential reaches the panel a 401 from the panel is surfaced without a refresh or a replay
00:06 +260: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: no watchools credential reaches the panel the User-Agent header key reaches the wire with its casing intact
  Skip: Needs magic with preserveHeaderCase, fluttersdk/magic#151 (branch fix/dio-preserve-header-case). It passes against the local override and fails against the published constraint, so it stays skipped until the commit that bumps `magic:` unskips it.
00:06 +260 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: endpoint and credentials the handshake sends the credential and no action
00:06 +261 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: endpoint and credentials the credential rides in the query map and never in the URL string
00:06 +262 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: endpoint and credentials every action reaches the panel under its own name
00:06 +263 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: endpoint and credentials the client addresses the stored panel and ignores server_info.url
00:06 +264 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: endpoint and credentials the header key is spelled exactly User-Agent, which ExoPlayer needs
00:06 +265 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: query shape the stream lists send the credential and the action and nothing else
00:06 +266 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: query shape get_short_epg sends stream_id and the limit the action honours
00:06 +267 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: query shape get_simple_data_table sends stream_id and no limit, being the unlimited sibling
00:06 +268 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: query shape get_vod_info sends vod_id
00:06 +269 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: response reading an object body arriving as a raw String still parses
00:06 +270 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: response reading a list body arriving as a raw String still parses
00:06 +271 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: response reading a 200 carrying the plain word blocked keeps the body and decodes to null
00:06 +272 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: response reading a transport failure surfaces as status 0 rather than a throw
00:06 +273 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: response reading an unknown action answering with an array reads as empty rather than unreadable
00:06 +274 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: response reading the EPG actions unwrap the epg_listings envelope
00:06 +275 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: get_simple_date_table fallback an empty data table retries the typo spelling
00:06 +276 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: get_simple_date_table fallback a populated data table does not retry
00:06 +277 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: get_simple_date_table fallback two empty answers return the documented spelling, which is the truthful one
00:06 +278 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: get_simple_date_table fallback a refused data table is surfaced rather than retried
00:06 +279 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: get_simple_date_table fallback a 200 carrying an unreadable body is NOT retried
00:06 +280 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/support/guide_clock_test.dart: FixedGuideClock reports the hour the fixtures are written around
00:06 +281 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/support/guide_clock_test.dart: FixedGuideClock never notifies, so a controller can subscribe without asking
00:06 +282 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/support/guide_clock_test.dart: TickingGuideClock starts on its anchor
00:06 +283 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/support/guide_clock_test.dart: TickingGuideClock advances one minute per minute
00:06 +284 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/support/guide_clock_test.dart: TickingGuideClock does not move within a minute
00:06 +285 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/support/guide_clock_test.dart: TickingGuideClock notifies once per minute rather than once per tick of anything
00:06 +286 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/support/guide_clock_test.dart: TickingGuideClock re-arms against elapsed time rather than drifting
00:06 +287 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/support/guide_clock_test.dart: TickingGuideClock passes midnight without wrapping
00:06 +288 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/support/guide_clock_test.dart: TickingGuideClock stops when disposed
00:07 +289 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: filtering starts on the whole line-up
00:07 +290 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: filtering a category narrows to that category and Tümü widens again
00:07 +291 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: filtering Favoriler shows only starred channels
00:07 +292 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: filtering search covers the name, the number and the programme on now
00:07 +293 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: filtering search is case and whitespace insensitive
00:07 +294 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: filtering a category and a query compose rather than replace
00:07 +295 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the frame caches hand back the same list until something changes
00:07 +296 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the frame caches are dropped by every mutation that changes what is visible
00:07 +297 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: sections follow the provider order rather than the alphabet
00:07 +298 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: sections cover every match exactly once
00:07 +299 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the counts every layout renders countLabel says kanal without a query and sonuç with one
00:07 +300 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the counts every layout renders noGuideNote counts the matches with no schedule and hides at zero
00:07 +301 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the counts every layout renders scheduled is the subset the time axis can draw
00:07 +302 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: selection starts on the first channel and its current programme
00:07 +303 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: selection selecting a channel points the programme at what is on now
00:07 +304 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: selection selecting a programme keeps both, which the time axis needs
00:07 +305 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: selection starring the selected channel keeps the selection on the new instance
00:07 +306 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: selection starring twice returns to unstarred
00:07 +307 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the view switch arrives on Şimdi and holds a switch to Zaman
00:07 +308 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the view switch switching view keeps the filters, which is why both read one controller
00:07 +309 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the view switch switching view keeps the selected channel and its programme
00:07 +310 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the editorial rails the two live rails answer questions only a schedule can
00:07 +311 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the editorial rails the soon rail is named for the horizon it actually uses
00:07 +312 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the editorial rails a channel with no guide appears in exactly one rail
00:07 +313 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the editorial rails an empty rail is dropped rather than rendered
00:07 +314 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: scope starts on the whole catalogue
00:07 +315 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: scope starts on the whole catalogue
00:07 +316 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: scope starts on the whole catalogue
00:07 +317 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: scope starts on the whole catalogue
00:07 +318 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: scope starts on the whole catalogue
00:07 +319 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: scope starts on the whole catalogue
00:07 +320 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: scope starts on the whole catalogue
00:07 +321 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: scope starts on the whole catalogue
00:07 +322 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: scope starts on the whole catalogue
00:07 +323 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: scope starts on the whole catalogue
00:07 +324 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the clock a clock it built itself is disposed with it
00:07 +325 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: scope films excludes series and series excludes films
00:07 +326 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the clock it stops listening when the controller closes
00:07 +327 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: scope the selection follows the scope rather than pointing off screen
00:07 +328 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the window the guide draws starts on the half hour before now
00:07 +329 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: scope a scope with matches keeps a selection that is still in it
00:07 +330 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the window the guide draws covers prime time end to end rather than fitting a viewport
00:07 +331 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: categories match the provider category OR the genre list
00:07 +332 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: categories match the provider category OR the genre list
00:07 +333 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the provider session an empty provider catalogue does not throw
00:07 +334 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the provider session an empty provider catalogue does not throw
00:07 +335 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: categories Favoriler shows only starred titles
00:07 +336 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the provider session provider channels reach matches and groups
00:07 +337 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the provider session provider channels reach matches and groups
00:07 +338 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: search reaches episode titles, not only the top level
00:07 +339 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: search reaches episode titles, not only the top level
00:07 +340 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the provider session starring on the provider path routes through the session
00:07 +341 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the provider session starring on the provider path routes through the session
00:07 +342 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: search an unmatched term empties the catalogue rather than throwing
00:07 +343 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the provider session now reads the session clock once a refresh has anchored one
00:07 +344 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: the counts every layout renders countLabel says başlık without a query and sonuç with one
00:07 +345 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the provider session a catalogue arriving after the first frame repaints the screen
00:07 +346 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the provider session a catalogue arriving after the first frame repaints the screen
00:07 +347 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: the title screen opens on the season the next episode is in, not season one
00:07 +348 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the provider session a fault arriving after the first frame repaints too
00:07 +349 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the provider session a fault arriving after the first frame repaints too
00:07 +350 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: the title screen opening a title leaves the catalogue filters alone
00:07 +351 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the provider session the tick subscription moves onto the session clock, so a minute change repaints
00:07 +352 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: the title screen selecting a season holds
00:07 +353 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: favourites starring keeps the selection on the new instance
00:07 +354 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: favourites starring an unselected title leaves the selection alone
00:07 +355 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: the rails Vitrin draws continueWatching is what was started and not finished
00:07 +356 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: the rails Vitrin draws sections cover every match exactly once
00:07 +357 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: the frame caches hand back the same list until something changes
00:07 +358 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: the frame caches are dropped by a scope change, a category change and a star
00:07 +359 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: the provider session a session with no credentials still yields the fixture catalogue
00:07 +360 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: the provider session an empty provider catalogue does not throw
00:07 +361 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: the provider session provider titles reach matches and categories
00:07 +362 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: the provider session a fault on the session surfaces without emptying matches
00:07 +363 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
00:07 +364 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
00:07 +364 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/app_config_test.dart: appConfig carries the application identity under the `app` key
Env: Loading environment variables from .env
Env: Environment variables loaded successfully
00:07 +365 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
00:07 +365 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/app_config_test.dart: appConfig reads the name from the environment rather than hardcoding it
Env: Loading environment variables from .env
Env: Environment variables loaded successfully
Env: Loading environment variables from .env
Env: Environment variables loaded successfully
00:07 +366 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
00:07 +366 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/app_config_test.dart: appConfig registers every provider the app boots
Env: Loading environment variables from .env
Env: Environment variables loaded successfully
00:07 +367 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:08 +368 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit bg-primary-hover resolves
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:08 +369 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit the scrim weights resolve to translucent black
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:08 +370 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: the focus ring ring-focus-ring carries the audited blue, not Wind default
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:08 +371 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: the focus ring focus is not the brand colour
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:08 +372 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: line clamp line-clamp-N sets maxLines and an ellipsis
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:08 +373 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: line clamp n-N, which the app used to spend, resolves to nothing
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind: unknown className 'n-3' was ignored.
00:08 +374 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: the destructive family, which is a button colour and not a text one bg-destructive-container resolves, which is what tints the fault disc
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:08 +375 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: the destructive family, which is a button colour and not a text one text-destructive resolves to nothing, and that shapes the design
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:08 +376 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: the destructive family, which is a button colour and not a text one warning is indistinguishable from the brand, so a fault cannot wear it
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:08 +377 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: the status tokens every layout spends the playback states resolve in dark mode
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:08 +378 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/wind_wiring_test.dart: renders a Wind widget through the local path dependency
00:08 +379 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/wind_wiring_test.dart: renders a Wind widget through the local path dependency
00:08 +380 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/wind_wiring_test.dart: renders a Wind widget through the local path dependency
00:08 +381 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/wind_wiring_test.dart: renders a Wind widget through the local path dependency
00:08 +382 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/wind_wiring_test.dart: renders a Wind widget through the local path dependency
00:08 +383 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/wind_wiring_test.dart: renders a Wind widget through the local path dependency
00:08 +384 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/wind_wiring_test.dart: renders a Wind widget through the local path dependency
00:08 +385 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/wind_wiring_test.dart: renders a Wind widget through the local path dependency
00:08 +386 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/wind_wiring_test.dart: renders a Wind widget through the local path dependency
00:08 +387 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/wind_wiring_test.dart: renders a Wind widget through the local path dependency
00:08 +388 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/wind_wiring_test.dart: renders a Wind widget through the local path dependency
00:09 +389 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/watchools_theme_test.dart: rendering resolves bg-primary to the brand gold in dark mode
00:09 +390 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/watchools_theme_test.dart: rendering resolves bg-primary to the brand gold in dark mode
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:09 +391 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/widget_test.dart: Magic app boots smoke test
00:09 +392 ~1: All tests passed!
