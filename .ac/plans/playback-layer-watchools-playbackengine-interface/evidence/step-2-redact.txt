00:00 +0: loading /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_credentials_test.dart
00:00 +0: base URL normalisation strips a trailing slash, because every later call concatenates onto it
00:00 +1: base URL normalisation keeps a panel path prefix, minus its trailing slash
00:00 +2: base URL normalisation rejects a base URL Http would concatenate onto the driver base_url
00:00 +3: base URL normalisation accepts https as readily as http
00:00 +4: base URL normalisation rejects credentials in the authority, which toString would print in full
00:00 +5: base URL normalisation rejects without naming the value, so a pasted password never reaches a log
00:00 +6: Vault round trip save() writes exactly one key and load() returns an equal record
00:00 +7: Vault round trip clear() removes the one key it wrote
00:00 +8: Vault round trip load() is null on a vault that never held a credential
00:00 +9: Vault round trip load() normalises the stored base URL the way the constructor does
00:00 +10: Vault round trip load() throws on a payload that is not a credential object
00:00 +11: Vault round trip load() throws when a field is missing or not a string
00:00 +12: Vault round trip load() throws without naming the field values, so nothing leaks
00:00 +13: toString names the provider and the user, and redacts the password
00:00 +14: describe drops the query and the credential path segments of a stream URL
00:00 +15: describe keeps scheme, host, port and the segments that are not a credential
00:00 +16: describe drops userInfo, the other place a URL carries a credential
00:00 +17: redact strips both secrets out of the FFmpeg reconnect warning that is the only lapse signal
00:00 +18: redact replaces every occurrence, since a retry line names the same URL twice
00:00 +19: redact leaves a line naming no secret exactly as it arrived
00:00 +20: redact redacts the password before the username, so a password containing the username still goes
00:00 +21: redact a provider that issues the password as the username redacts once, not twice over
00:00 +22: redact redacts the percent-encoded spelling a URL path carries
00:00 +23: redact redacts the query spelling too, which writes a space as + rather than %20
00:00 +24: redact an empty secret is skipped, because replaceAll of it matches between every character
00:00 +25: All tests passed!
Analyzing player-reconnect...                                   
No issues found! (ran in 11.2s)
00:00 +0: loading /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart
00:00 +0: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: the generated catalogue is not empty
00:00 +1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: Artwork renders at desktop
00:00 +2: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: Artwork renders at desktop
00:00 +3: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: Artwork renders at desktop
00:00 +4: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: Artwork renders at desktop
00:00 +5: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: Artwork renders at desktop
00:00 +6: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: Artwork renders at desktop
00:00 +7: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: Artwork renders at desktop
00:00 +8: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: Artwork renders at desktop
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +9: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/components/provider_notice_test.dart: every fault says something different, and says it in three parts
00:01 +9: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: Artwork renders on a phone
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +10: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/components/provider_notice_test.dart: every fault says something different, and says it in three parts
00:01 +10: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: ChannelMark renders at desktop
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +11: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/components/provider_notice_test.dart: every fault says something different, and says it in three parts
00:01 +11: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: ChannelMark renders on a phone
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +12: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/components/provider_notice_test.dart: every fault says something different, and says it in three parts
00:01 +12: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: EpisodeRow renders at desktop
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +13: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/components/provider_notice_test.dart: every fault says something different, and says it in three parts
00:01 +13: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: EpisodeRow renders on a phone
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:01 +14: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
00:02 +15: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:02 +15: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: FactChip renders at desktop
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:02 +16: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:02 +16: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: FactChip renders on a phone
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:02 +17: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:02 +18: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:02 +18: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: FavouriteButton renders at desktop
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:02 +19: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:02 +20: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:02 +21: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:02 +21: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: FavouriteButton renders on a phone
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:02 +22: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:02 +23: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:02 +23: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: PlayProgress renders at desktop
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:02 +24: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:02 +25: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:02 +25: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: PlayProgress renders on a phone
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:02 +26: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:02 +27: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:02 +27: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: ProviderNotice renders at desktop
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:02 +28: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:02 +28: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: ProviderNotice renders on a phone
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:02 +29: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:02 +29: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
00:02 +29: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: StatusBadge renders at desktop
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:02 +29: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
00:02 +30: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:02 +30: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:02 +30: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: StatusBadge renders on a phone
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:02 +31: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice and keeps the toolbar when the session carries a fault
00:03 +31: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: TitlePoster renders at desktop
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:03 +32: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:03 +32: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi renders the notice alone when the fault AND the result are both empty
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:03 +33: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:03 +33: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: TitlePoster renders on a phone
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
00:03 +34: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:03 +34: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/preview_catalogue_test.dart: TitlePoster renders on a phone
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:03 +34: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi still renders the empty state when there is no fault
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:03 +35: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:03 +35: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi routes the notice action through GuideController.reload
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
00:03 +36: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:03 +36: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Şimdi routes the notice action through GuideController.reload
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:03 +37: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:03 +37: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Zaman renders the notice and keeps the toolbar when the session carries a fault
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:04 +38: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:04 +38: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Zaman renders the notice alone when the fault AND the result are both empty
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:04 +39: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:04 +39: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Zaman still renders the empty state when there is no fault
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:04 +40: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 1440
00:04 +40: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: the live views (Şimdi, Zaman) Zaman routes the notice action through GuideController.reload
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:04 +41: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:04 +41: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders an empty result set at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:04 +42: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:04 +42: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: Vitrin renders the notice and keeps the toolbar when the session carries a fault
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:04 +43: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:04 +43: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders an empty favourites filter at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:04 +43: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:04 +44: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:04 +44: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: Vitrin renders the notice alone when the fault AND the result are both empty
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:05 +45: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +45: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
00:05 +46: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +46: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 1440
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:05 +46: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: Vitrin still renders the empty state when there is no fault
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:05 +47: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +48: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +49: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +50: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +51: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +52: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +53: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +53: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: Vitrin routes the notice action through LibraryController.reload
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
00:05 +54: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +54: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: Vitrin routes the notice action through LibraryController.reload
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:05 +55: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +56: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +57: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +58: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +59: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +60: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +61: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +62: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +63: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +64: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +65: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +66: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +67: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +67: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: Perde renders the notice when the session carries a fault, even with a title selected
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:05 +68: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +68: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: Perde renders the notice alone when the fault AND the catalogue are both empty
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:05 +69: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +70: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +70: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: Perde still renders the no-selection arm when there is no fault
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:05 +70: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a channel with no guide selected at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:05 +71: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:05 +71: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/provider_fault_test.dart: Perde routes the notice action through LibraryController.reload
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:05 +72: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:06 +73: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:06 +73: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:06 +74: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Şimdi survives the query that empties the line-up
00:06 +74: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:07 +75: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_test.dart: CatalogueStore, the schema and the round trip round-trips 1000 channels as equal value objects
00:07 +75: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: and survives the way back to results
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
00:07 +76: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 1440
00:07 +76: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: and survives the way back to results
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:07 +77: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 1440
00:07 +78: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 1440
00:07 +79: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 1440
00:07 +80: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 1440
00:07 +81: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 1440
00:07 +82: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 1440
00:07 +83: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 1440
00:07 +84: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 1440
00:07 +85: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 1440
00:07 +86: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 1440
00:07 +87: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 1440
00:07 +88: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 1440
00:07 +89: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 1440
00:07 +90: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 1440
00:07 +91: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 1440
00:07 +92: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 1440
00:07 +93: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 1440
00:07 +94: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:07 +94: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty result set at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:07 +95: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:07 +95: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty favourites filter at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
Shell: SCALE channels: 2976 rows in 103 ms
Shell: SCALE titles:   38247 rows in 341 ms
Shell: SCALE combined: 444 ms (redesign threshold, not asserted: ~2000 ms)
00:07 +96: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:08 +96: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:08 +97: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:08 +97: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Vitrin survives it too
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:08 +98: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/provider/catalogue_store_scale_test.dart: writes the real catalogue size
00:08 +99: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 1440
00:08 +100: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 1440
00:08 +101: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 1440
00:08 +102: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 1440
00:08 +103: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 1440
00:08 +104: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 1440
00:08 +105: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Vitrin survives it too
00:08 +106: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Vitrin survives it too
00:08 +107: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Vitrin survives it too
00:08 +108: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Vitrin survives it too
00:08 +109: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Vitrin survives it too
00:08 +109: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:08 +110: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Vitrin survives it too
00:08 +111: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/models/title_item_test.dart: TitleItem toggling a favourite keeps every other field
00:08 +112: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders with the other view selected at 1440
00:08 +113: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders with the other view selected at 1440
00:08 +113: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Zaman, the control, was never affected
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
00:08 +114: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders with the other view selected at 1440
00:08 +114: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Zaman, the control, was never affected
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:09 +115: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders with the other view selected at 1440
00:09 +116: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders with the other view selected at 1440
00:09 +117: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders with the other view selected at 1440
00:09 +118: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders with the other view selected at 1440
00:09 +119: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders with the other view selected at 1440
00:09 +120: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders with the other view selected at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:09 +121: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders with the other view selected at 1440
00:09 +122: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/search_focus_test.dart: Zaman, the control, was never affected
00:09 +122: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:10 +123: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders the whole line-up at 414
00:10 +124: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders an empty result set at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:10 +125: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders an empty favourites filter at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
00:10 +126: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders an empty favourites filter at 414
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:10 +127: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders an empty favourites filter at 414
00:10 +128: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders an empty favourites filter at 414
00:10 +129: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders an empty favourites filter at 414
00:10 +130: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/models/channel_test.dart: Channel.fromXtream carries the archive window and the stream id through a favourite toggle
00:10 +131: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:10 +132: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:10 +133: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:10 +134: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:10 +135: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:10 +136: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:10 +137: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +138: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +139: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +140: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +141: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +142: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +143: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +144: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +145: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +146: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +147: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +148: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +149: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +150: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +151: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +152: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +153: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +154: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +155: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +156: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +157: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +158: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +159: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +160: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +161: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a starred line-up at 414
00:11 +162: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders a channel with no guide selected at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:11 +163: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:11 +164: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +165: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +166: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +167: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +168: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +169: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +170: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +171: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +172: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +173: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +174: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +175: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +176: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +177: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +178: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +179: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +180: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +181: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +182: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +183: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +184: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +185: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +186: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +187: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +188: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +189: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +190: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +191: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +192: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +193: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +194: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +195: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:11 +196: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Şimdi renders with the other view selected at 414
00:12 +197: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_credentials_test.dart: base URL normalisation strips a trailing slash, because every later call concatenates onto it
00:12 +197: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:12 +198: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +199: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +200: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +201: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +202: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +203: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +204: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +205: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +206: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +207: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +208: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +209: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +210: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +211: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +212: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +213: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +214: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +215: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +216: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +217: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +218: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +219: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +220: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +221: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +222: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders the whole line-up at 414
00:12 +223: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: no watchools credential reaches the panel the registered provider driver ships no Authorization header with a token cached
00:12 +223: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty result set at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:12 +224: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: no watchools credential reaches the panel the registered provider driver ships no Authorization header with a token cached
00:12 +225: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty favourites filter at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:12 +226: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty favourites filter at 414
00:12 +227: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty favourites filter at 414
00:12 +228: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty favourites filter at 414
00:12 +229: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty favourites filter at 414
00:12 +229: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: no watchools credential reaches the panel the User-Agent header key reaches the wire with its casing intact
  Skip: Needs magic with preserveHeaderCase, fluttersdk/magic#151 (branch fix/dio-preserve-header-case). It passes against the local override and fails against the published constraint, so it stays skipped until the commit that bumps `magic:` unskips it.
00:12 +229 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders an empty favourites filter at 414
00:12 +230 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_client_test.dart: endpoint and credentials the handshake sends the credential and no action
00:12 +231 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +232 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +233 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +234 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +235 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +236 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +237 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
00:12 +238 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:12 +239 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +240 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +241 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +242 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +243 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +244 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +245 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +246 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +247 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +248 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +249 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +250 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +251 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +252 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +253 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +254 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +255 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a starred line-up at 414
00:12 +256 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_stream_url_test.dart: extension yields m3u8 when the channel excludes ts, as channel 07 does
00:12 +257 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 414
00:12 +258 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 414
00:12 +259 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 414
00:12 +260 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:12 +261 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 414
00:12 +262 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 414
00:12 +263 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 414
00:13 +264 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 414
00:13 +265 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 414
00:13 +266 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 414
00:13 +267 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 414
00:13 +268 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 414
00:13 +269 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 414
00:13 +270 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 414
00:13 +271 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 414
00:13 +272 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders a channel with no guide selected at 414
00:13 +273 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the live views Zaman renders with the other view selected at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:13 +274 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders the whole catalogue at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:13 +275 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders the whole catalogue at 1440
00:13 +276 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders the whole catalogue at 1440
00:13 +277 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders the whole catalogue at 1440
00:13 +278 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders the whole catalogue at 1440
00:13 +279 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/library_controller_test.dart: categories match the provider category OR the genre list
00:13 +280 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
00:13 +281 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
00:13 +282 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
00:13 +283 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:13 +284 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:13 +285 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
00:13 +286 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
00:13 +287 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
00:13 +288 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
00:13 +289 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
00:13 +290 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
00:13 +291 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
00:13 +292 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
00:13 +293 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
00:13 +294 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
00:13 +295 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
00:13 +296 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
00:13 +297 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
00:13 +298 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
00:13 +299 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
00:13 +300 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 1440
00:13 +301 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 1440
00:13 +302 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 1440
00:13 +303 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:13 +304 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 1440
00:13 +305 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: filtering starts on the whole line-up
00:13 +306 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:13 +307 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:13 +308 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
00:13 +309 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:13 +310 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:13 +311 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:13 +312 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:13 +313 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:13 +314 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:13 +315 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:13 +316 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:13 +317 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:13 +318 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:13 +319 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +320 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +321 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +322 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +323 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +324 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +325 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +326 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +327 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +328 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +329 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +330 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +330 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/app_config_test.dart: appConfig carries the application identity under the `app` key
Env: Loading environment variables from .env
00:14 +331 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +332 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +333 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +334 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +335 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +336 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +337 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +338 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +339 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +340 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +341 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +342 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +343 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +344 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +345 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +345 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/app_config_test.dart: appConfig carries the application identity under the `app` key
Env: Environment variables loaded successfully
00:14 +346 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +347 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 1440
00:14 +348 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/controllers/guide_controller_test.dart: the provider session a fault on the session surfaces without emptying matches
00:14 +349 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/app_config_test.dart: appConfig reads the name from the environment rather than hardcoding it
00:14 +350 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/app_config_test.dart: appConfig reads the name from the environment rather than hardcoding it
Env: Loading environment variables from .env
Env: Environment variables loaded successfully
Env: Loading environment variables from .env
Env: Environment variables loaded successfully
00:14 +351 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders the whole catalogue at 414
00:14 +352 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders the whole catalogue at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:14 +353 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders the whole catalogue at 414
00:14 +353 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/app_config_test.dart: appConfig registers every provider the app boots
Env: Loading environment variables from .env
Env: Environment variables loaded successfully
00:14 +354 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders the whole catalogue at 414
00:14 +355 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders the whole catalogue at 414
00:14 +356 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders the whole catalogue at 414
00:14 +357 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty result set at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:14 +358 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders series only at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:14 +359 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the catalogue Vitrin renders an empty continue-watching at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:14 +360 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:14 +361 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a film at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:14 +362 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
00:14 +362 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a title with nothing filled in at 1440
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:14 +363 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
00:14 +363 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a series at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:14 +364 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
00:14 +364 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a film at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:15 +365 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
00:15 +365 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen Perde renders a title with nothing filled in at 414
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:15 +366 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
00:15 +366 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/ui/layouts/screens_test.dart: the title screen every season of a series renders
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:15 +366 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:15 +367 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
00:15 +368 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
00:15 +369 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
00:15 +370 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
00:15 +371 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
00:15 +372 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
00:15 +373 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
00:15 +374 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
00:15 +375 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
00:15 +376 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
00:15 +377 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit text-primary resolves to the dark-mode gold
00:15 +378 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/wind_wiring_test.dart: renders a Wind widget through the local path dependency
00:15 +378 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit bg-primary-hover resolves
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:15 +379 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/wind_wiring_test.dart: renders a Wind widget through the local path dependency
00:15 +379 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: colour tokens the generator does not emit the scrim weights resolve to translucent black
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:15 +380 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/wind_wiring_test.dart: renders a Wind widget through the local path dependency
00:15 +380 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: the focus ring ring-focus-ring carries the audited blue, not Wind default
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:15 +381 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/wind_wiring_test.dart: renders a Wind widget through the local path dependency
00:15 +381 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: the focus ring focus is not the brand colour
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:15 +382 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/wind_wiring_test.dart: renders a Wind widget through the local path dependency
00:15 +382 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: line clamp line-clamp-N sets maxLines and an ellipsis
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:15 +383 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/wind_wiring_test.dart: renders a Wind widget through the local path dependency
00:15 +383 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: line clamp n-N, which the app used to spend, resolves to nothing
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind: unknown className 'n-3' was ignored.
00:15 +384 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/wind_wiring_test.dart: renders a Wind widget through the local path dependency
00:15 +384 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: the destructive family, which is a button colour and not a text one bg-destructive-container resolves, which is what tints the fault disc
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:15 +385 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/watchools_theme_test.dart: rendering resolves bg-primary to the brand gold in dark mode
00:15 +386 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/watchools_theme_test.dart: rendering resolves bg-primary to the brand gold in dark mode
00:15 +386 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: the destructive family, which is a button colour and not a text one text-destructive resolves to nothing, and that shapes the design
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:15 +387 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/watchools_theme_test.dart: rendering resolves bg-primary to the brand gold in dark mode
00:15 +388 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/watchools_theme_test.dart: rendering resolves bg-primary to the brand gold in dark mode
00:15 +388 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: the destructive family, which is a button colour and not a text one warning is indistinguishable from the brand, so a fault cannot wear it
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:15 +389 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/watchools_theme_test.dart: rendering resolves bg-primary to the brand gold in dark mode
00:15 +389 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/token_resolution_test.dart: the status tokens every layout spends the playback states resolve in dark mode
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:15 +389 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/watchools_theme_test.dart: rendering resolves bg-primary to the brand gold in dark mode
Wind alias 'bg-surface' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-surface-container-high' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-muted' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fg-disabled' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-accent' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-border-subtle' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-destructive' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-destructive-container' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-success' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-warning' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-live' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-live-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-live-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-recording' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-recording-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-recording-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-catchup' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-catchup-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-catchup-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-epg-now' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-epg-now-soft' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-epg-now-soft-foreground' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-fact' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-primary' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-primary-hover' shadows a built-in token; the built-in is now unreachable.
Wind alias 'ring-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-scrim-strong' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-on-inverse' shadows a built-in token; the built-in is now unreachable.
Wind alias 'border-color-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'text-focus-ring' shadows a built-in token; the built-in is now unreachable.
Wind alias 'bg-focus-ring' shadows a built-in token; the built-in is now unreachable.
00:15 +390 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/config/watchools_theme_test.dart: rendering resolves bg-primary to the brand gold in dark mode
00:16 +391 ~1: /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/widget_test.dart: Magic app boots smoke test
00:16 +392 ~1: All tests passed!
