fix(playback): name the channel a refusal is about

Found by walking the running app rather than by a test. The controller was
changed to keep `_channel` through an unplayable channel so the screen could
name it, and the screen then dropped the name anyway: the `unplayable` branch
replaced the channel line instead of adding to it, so the panel read "Bu kanal
oynatılamıyor" with no channel on it. The fix bought nothing visible until
this, which is the reason a screen gets walked and not only tested.

The walk itself is what the widget tests cannot reach, and it is the check
both of this round's critical fixes needed: `/` to `/izle` and back, then a
second visit. The route pushes, the screen renders, the back affordance
returns, and the second visit is clean, which is the path that previously sent
a forgotten view id. No exception is raised on either visit, and the chrome is
a content-width panel over an untouched frame.
