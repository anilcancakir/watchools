Step 9 gates, run 2026-09-11 from the network-resolver worktree.

$ flutter analyze --fatal-infos --fatal-warnings
No issues found! (ran in 3.5s)

$ dart format --set-exit-if-changed lib test
Formatted 165 files (0 changed)

$ flutter test --coverage
00:26 +636 ~1: All tests passed!

$ FLUTTER_COVERAGE_MINIMUM=90 python3 <the inline block from .github/workflows/ci.yml:115-143>
Flutter line coverage (scaffold excluded): 2850/3007 = 94.8%, floor 90.0%

$ flutter build macos --debug
Xcode is fetching Swift Package Manager dependencies. This may take several minutes...
  Fetching from https://github.com/mpvkit/MPVKit.git (cached)...
Building macOS application...
✓ Built build/macos/Build/Products/Debug/Watchools.app
[exited with code 0]

$ flutter build web --no-pub
Compiling lib/main.dart for the Web...                             44.1s
✓ Built build/web

$ git status --porcelain
?? macos/Runner.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/
?? macos/Runner.xcworkspace/xcshareddata/swiftpm/

Both untracked paths are Xcode's own, written by the macOS build. `CLAUDE.md`
already records that `.gitignore`'s `.swiftpm/` does not match Xcode's
`xcshareddata/swiftpm/`; neither is this plan's output and neither is committed.
`pubspec.lock` and `macos/Flutter/GeneratedPluginRegistrant.swift` were both
checked after the macOS build and neither was rewritten.
