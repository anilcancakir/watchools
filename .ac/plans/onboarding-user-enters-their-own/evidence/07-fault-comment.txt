Test Results for xtream_account and Related Tests
===================================================

BEFORE: 25 tests (24 passed)
AFTER: 126 tests (125 passed, 1 skipped)

Specific count for xtream_account_test.dart:
- All 25 tests passed unchanged
  Tests 0-24: all XtreamAccount and classifyProviderFault tests

Full xtream directory tests:
- xtream_account_test.dart: 25 tests passed
- xtream_json_test.dart: 33 tests passed
- xtream_credentials_test.dart: 32 tests passed
- xtream_client_test.dart: 20 tests passed, 1 skipped
- xtream_stream_url_test.dart: 15 tests passed
Total: 125 passed, 1 skipped, 0 failed


Git Diff
========

The comment rewrite at lib/app/protocol/xtream/xtream_account.dart lines 222-231:

--- OLD (with stale clause):
  // 3. The generic denial with no account behind it, which is the first-launch
  //    case: the handshake itself came back as unparseable text. That says
  //    nothing whatsoever about the credential, so it must not be read as
  //    `expired`, the one fault that withholds the retry
  //    (`provider_notice.dart`'s button routes to settings for it and nowhere
  //    useful, since no onboarding screen exists). A blocked address, a
  //    blocked user agent and a reverse proxy's HTML error page all land here,
  //    and all three are recoverable.

+++ NEW (with updated justification):
  // 3. The generic denial with no account behind it, which is the first-launch
  //    case: the handshake itself came back as unparseable text. That says
  //    nothing whatsoever about the credential, so it must not be read as
  //    `expired`, the one fault that withholds the retry. Retrying is the
  //    right offer for a denial the panel has not explained; if it persists,
  //    the onboarding screen can walk the user through entering their
  //    credentials correctly. A blocked address, a blocked user agent and a
  //    reverse proxy's HTML error page all land here, and all three are
  //    recoverable.


Key Changes:
1. REMOVED: "(`provider_notice.dart`'s button routes to settings for it and nowhere useful, since no onboarding screen exists)"
2. ADDED: "Retrying is the right offer for a denial the panel has not explained; if it persists, the onboarding screen can walk the user through entering their credentials correctly."

Control Flow Unchanged:
- No branch moved (if statements unchanged)
- No return statement moved
- Behaviour remains identical (still returns ProviderFault.throttled)
- All 25 tests in xtream_account_test.dart pass unchanged
- All 126 tests in test/app/protocol/xtream/ pass (125 passed, 1 skipped)
