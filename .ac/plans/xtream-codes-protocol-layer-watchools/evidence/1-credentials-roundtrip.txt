00:00 +0: loading /Users/anilcan/Code/watchools/.claude/worktrees/player-reconnect/test/app/protocol/xtream/xtream_credentials_test.dart
00:00 +0: base URL normalisation strips a trailing slash, because every later call concatenates onto it
00:00 +1: base URL normalisation keeps a panel path prefix, minus its trailing slash
00:00 +2: base URL normalisation rejects a base URL Http would concatenate onto the driver base_url
00:00 +3: base URL normalisation accepts https as readily as http
00:00 +4: Vault round trip save() writes exactly one key and load() returns an equal record
00:00 +5: Vault round trip clear() removes the one key it wrote
00:00 +6: Vault round trip load() is null on a vault that never held a credential
00:00 +7: Vault round trip load() normalises the stored base URL the way the constructor does
00:00 +8: Vault round trip load() throws on a payload that is not a credential object
00:00 +9: Vault round trip load() throws when a field is missing or not a string
00:00 +10: Vault round trip load() throws without naming the field values, so nothing leaks
00:00 +11: toString names the provider and the user, and redacts the password
00:00 +12: describe drops the query and the credential path segments of a stream URL
00:00 +13: describe keeps scheme, host, port and the segments that are not a credential
00:00 +14: describe drops userInfo, the other place a URL carries a credential
00:00 +15: All tests passed!
